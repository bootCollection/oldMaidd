#include <sstream>
#include "../include/card.h"
#include "../include/deck.h"
#include "../include/playerHand.h"
#include "gtest/gtest.h"

//TEST(PlayerHand, constructor) {
//  Deck D,C;

  //EXPECT_NO_THROW(PlayerHand A(D, 5));
  //EXPECT_THROW(PlayerHand B(C,55));
//}

/*TEST(PlayerHand, sizeTest) {
  Deck D;
  PlayerHand A(D, 5)
  EXPECT_EQ(A.size(), 5);
}*/
